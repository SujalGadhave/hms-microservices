package com.hms.analytics.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SecurityConfig {
    // Security disabled for analytics module (autoconfig excluded)
}