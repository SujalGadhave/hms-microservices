server:
  port: ${SERVER_PORT:8091}

spring:
  application:
    name: admission-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:admission_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Sujal@123}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
